# Project Guidelines

## Styling

- **Always use Tailwind CSS classes only** for styling components in the `svk_recs_client` project
- Do not use `<style>` blocks or CSS-in-JS
- All styling should be done inline using Tailwind utility classes
